TODO:

- Lookup tables for insn lowering

- Test lowering with some simple inputs: wire it up to dump blocks

- Hook up regalloc to lowered blocks; replace regs with real regs (and insert
  moves/spills/fills where necessary)

- Encoding backend
